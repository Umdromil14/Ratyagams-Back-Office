import { useCallback, useState, forwardRef } from "react";
import "./style.css";

function Container() {
  return (
    <div className={`padding-left-auto`}>
      <div className={`green-rounded-box`} />
    </div>
  );
}

export default Container;
